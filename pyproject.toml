[project]
name = "reports-router"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.19.0",
    "faststream[cli,redis]>=0.5.37",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
